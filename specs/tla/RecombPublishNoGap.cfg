\* The reader claim. Replacing a legacy symlink under force=True leaves a gap
\* between unlink and rename, so the symlink start is excluded here.
CONSTANTS
    Writers = {w1, w2}
    Names = {c1, c2}
    Stray = s
    InitKinds = {"absent", "dir"}

SPECIFICATION Spec

INVARIANT NoGap
